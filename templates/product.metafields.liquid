{% layout none %}

{% assign variant = product.selected_variant %}

{%- if product -%}
 {
   "product": {{ product.metafields.custom_fields | json }},
   "variant": {{ variant.metafields.custom_fields | json }}
 }
{%- endif -%}
